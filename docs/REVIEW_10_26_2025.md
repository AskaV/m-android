# Code review

- no linters
- no readme file
- app/src/main/OpenSans-SemiBold.ttf seems to be redundant
- ContactsRepository should rather accept a content resolver directly if that is the only reason why context was passed.
- The presentation and data layers should not communicate with one another, use domain + DI to reduce dependency between those
- UserPreferencesKeys is only used in UserPreferences class, suggest to convert to companion object
- Avoid passing context directly, replace with what the context is used for
- What's the point in using AppText? 